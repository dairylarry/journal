# env files — never commit secrets
.env
.env.*
frontend/.env
frontend/.env.*

# backfill scripts contain hardcoded credentials
scripts/backfill-entry.mjs

# build output — CI builds this, don't commit
frontend/dist/

# dependencies
node_modules/
frontend/node_modules/
